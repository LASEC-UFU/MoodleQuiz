import 'package:flutter/material.dart';

/// [DESCONTINUADO] Setup page não é mais necessária.
/// O dataid do mq_state é descoberto automaticamente via busca no curso.
class SetupPage extends StatelessWidget {
  const SetupPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Esta página não é mais utilizada.'),
      ),
    );
  }
}
